import React from "react";
import { Button } from "./ui/button";

type Props = {};

function ShareButton({}: Props) {
  return <Button variant={"outline"} className="text-xl">𝕏</Button>;
}

export default ShareButton;
